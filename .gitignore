/result/
/model/
/fig/

# Simulator installs (created by scripts/setup_sims.sh — large, machine-specific)
/sims/

# Local config overrides (paths differ per machine — keep data/config.yaml
# committed as the template, ignore machine-specific copies if you make one)
/data/config.local.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Poetry / venvs / uv
.venv/
poetry.lock.bak

# IDE
.idea/
.vscode/
*.swp
